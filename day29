arr = [2, 4, 5, 7, 9, 11]
k = int(input("Enter value of k: "))

for i in range(len(arr)):
    for j in range(i + 1, len(arr)):
        if arr[i] + arr[j] == k:
            print(arr[i], "+", arr[j], "=", k)
