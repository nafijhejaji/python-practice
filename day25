import math

start = int(input("Start: "))
end = int(input("End: "))

for num in range(start, end + 1):
    temp = num
    total = 0

    while temp > 0:
        digit = temp % 10
        total += math.factorial(digit)
        temp //= 10

    if total == num:
        print(num, end=" ")
